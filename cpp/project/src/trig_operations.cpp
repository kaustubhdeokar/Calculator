#include "trig_operations.h"
#include <cmath>

double sincalc(double x) noexcept
{
    return std::sin(x);
}

double coscalc(double x)noexcept
{
    return std::cos(x);
}

double tancalc(double x)noexcept
{
    return std::tan(x);
}
